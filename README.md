# E-Commerce Management System

Система управления товарами и категориями для интернет-магазина.

## Функциональность

### Реализовано в этом задании:

1. **Классы моделей**:
   - `Product` - товар с атрибутами: название, описание, цена, количество
   - `Category` - категория товаров с атрибутами: название, описание, список товаров

2. **Атрибуты класса Category**:
   - `category_count` - счетчик количества категорий
   - `product_count` - счетчик количества товаров
   - Автоматическое обновление при создании объектов

3. **Загрузка данных**:
   - Поддержка загрузки из JSON-файла
   - Автоматическое создание объектов из данных

4. **Тестирование**:
   - Полное покрытие тестами (>75%)
   - Тесты инициализации объектов
   - Тесты подсчета количества
   - Тесты загрузки данных

## Установка

1. Установите Poetry:

pip install poetry


## Новая функциональность

### Наследование классов товаров

Добавлены два новых класса-наследника от базового класса `Product`:

1. **Smartphone** - представляет смартфоны с дополнительными атрибутами:
   - `efficiency` - производительность
   - `model` - модель
   - `memory` - объем встроенной памяти
   - `color` - цвет

2. **LawnGrass** - представляет газонную траву с дополнительными атрибутами:
   - `country` - страна-производитель
   - `germination_period` - срок прорастания (в днях)
   - `color` - цвет


#### 1. Абстрактные базовые классы
- **BaseProduct** - абстрактный базовый класс для всех продуктов
- **BaseContainer** - абстрактный базовый класс для контейнеров (категорий и заказов)

#### 2. Класс-миксин для логирования
- **ReprMixin** - автоматически логирует создание объектов с их параметрами
- При создании любого продукта выводится информация в консоль

#### 3. Система заказов
- **Order** - класс для представления заказов товаров
- Поддержка расчета общей стоимости заказа

#### 4. Расширенное наследование
- Все классы продуктов теперь наследуются от BaseProduct
- Category и Order наследуются от BaseContainer
- Сохранена обратная совместимость с существующим кодом

#### 5. Полное тестовое покрытие
- Написаны тесты для новой функциональности
- Покрытие кода тестами > 75%
- Все существующие тесты продолжают работать
### Ограничения сложения товаров

Реализована проверка типов при сложении товаров:
- Можно складывать только товары одного класса
- При попытке сложить товары разных классов возникает `TypeError`

### Защита от добавления невалидных объектов

Метод `add_product` категории теперь проверяет, что добавляемый объект является экземпляром класса `Product` или его наследников.

### Тестирование

Добавлены comprehensive тесты для:
- Наследования классов
- Ограничений сложения товаров
- Проверки типов при добавлении в категории
- Функциональности новых классов


##  Использование

```python
# Создание продукта с логированием
product = Product("Наушники", "Беспроводные", 199.99, 10)
# В консоль: Создан объект Product с параметрами: Product(name='Наушники', price=199.99, quantity=10)

# Создание заказа
order = Order(product, 2)
print(order)  # Заказ: Наушники, количество: 2, итого: 399.98 руб.
✅ Критерии выполнения
Реализованы абстрактные базовые классы

Добавлен класс-миксин для логирования

Реализована система заказов

Написаны comprehensive тесты

Сохранена обратная совместимость

Код соответствует PEP 8

Покрытие тестами > 75%

 Запуск тестов
bash
# Все тесты
pytest tests/ -v

# С покрытием кода
pytest --cov=src --cov-report html tests/

# Проверка типов
mypy .
 Примечания
Проект демонстрирует использование:

Абстрактных классов и методов

Множественного наследования

Миксинов

Полного тестирования

Аннотаций типов